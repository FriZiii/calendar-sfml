#pragma once
int zellerArgorithm(int month, int year); //Which returns which day of the week is the first day of the month

int DaysCount(int month, int year); //Calculate how many days in a month are leap years